apiVersion: hyperspike.io/v1
kind: MigrateRepo
metadata:
  labels:
    app.kubernetes.io/name: gitea-operator
    app.kubernetes.io/managed-by: kustomize
  name: migraterepo-sample
spec:
  # TODO(user): Add fields here
